# in this script we will be able to navigate the fake file system

# files : [file / directory's name, the rest of the content]
# each directory / file is a list with the second element separating
# the first element is the file / directory's name
def loadFiles(file)